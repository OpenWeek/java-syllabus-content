accessible: true
author: Jean-Martin Vlaeminck
context: |+
    Le code présenté dans le syllabus, et reproduit ci-dessous, contient un certain nombre d'erreurs liées aux boucles. Corrigez-les !

    .. code-block:: C

            // Effectue la somme sur chaque ligne des n premiers nombres multiples de 1, 2, 3, ..., m
            // int n = 17;
            // int m = 6;
            for (int i = 0; i <= n; i++) {
                int sum = 0;
                for (int j = 0; i <= m; j++) {
                    sum = sum + j * m;
                }
                System.out.println("Somme des " + n + " premiers nombres multiples de " + j + " : " + sum);
            }

environment: java7
evaluate: best
groups: false
limits:
    output: '2'
    memory: '300'
    time: '30'
name: 'Boucles for imbriquées : correction'
network_grading: false
problems:
    q1:
        type: code
        language: Java
        header: |
            Écrivez la version corrigée du code ci-dessus.
        name: Correction de code
stored_submissions: 0
submission_limit:
    amount: -1
    period: -1
weight: 1.0
